node_modules/
# Scratch files are included in S3 snapshots, but not in Git.
tmp/
# Credentials stay local, including in the private repository.
.env
.env.*
!.env.example
credentials
credentials.*
*.pem
*.key
.DS_Store
